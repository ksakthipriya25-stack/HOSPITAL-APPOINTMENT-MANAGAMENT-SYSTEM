pipeline {
    agent any
    stages {
        stage('Test') { steps { echo 'Testing Hospital Appointment System' } }
        stage('Build') { steps { echo 'Building Project' } }
        stage('Deploy') { steps { echo 'Deploy Successful' } }
    }
}

